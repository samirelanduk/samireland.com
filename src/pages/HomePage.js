import React from "react";
import Base from "./Base";

const HomePage = () => {
  return (
    <Base className="home-page">
      Home
    </Base>
  );
};

HomePage.propTypes = {
  
};

export default HomePage;