import React from "react";
import Base from "./Base";

const AboutPage = () => {
  return (
    <Base className="about-page">
      About
    </Base>
  );
};

AboutPage.propTypes = {
  
};

export default AboutPage;